# Progressive Execution Agent - Intake Source

## Source Type
spoken_012

## Raw Input Summary
012 identified a reusable FoundUp hidden inside the Fukui E-Singularity field workflow: an AI-guided execution system that carries the complexity of a real-world project while exposing only the highest-value next action to a minimally experienced founder. The system maintains a whole-project dependency graph and evidence ledger, invokes reusable WRE SKILLz for field discovery and stakeholder work, and progressively discloses only the immediate task to the human operator.

The initial proving ground is community feasibility: guide a founder through village discovery interviews, capture evidence, extract claims/conditions/referrals, update project state, and select the next best action without requiring the founder to understand the full infrastructure project.

## Inferred FoundUp Name
Progressive Execution Agent (provisional descriptive name)

## Proposed FoundUp ID
progressive_execution_agent

## Assumptions
- This is a reusable FoundUp, not a Fukui-specific feature.
- The Fukui E-Singularity project is the first PoC environment, not the product boundary.
- WRE remains the execution/orchestration authority; this FoundUp must not create a duplicate skill engine.
- WSP 95 governs creation and promotion of SKILLz.
- Project state is evidence-driven and dependency-aware; the human UI may be sequential even though the underlying project graph is not.

## Unresolved Questions
- Final public/product name.
- Whether the eventual PWA frontend remains in the monorepo or becomes an external deployment sleeve after the PoC proves the contract.
- Exact module boundary between FoundUp-specific graph/evidence logic and reusable WRE infrastructure.

## Duplicate Discovery Status
NEW_FOUNDUP

## Provenance Note
Derived from 012/0102 architecture discussion on 2026-08-25 while decomposing the Fukui E-Singularity execution workflow into reusable capability-driven SKILLz and an evidence-ledger/next-best-action loop.