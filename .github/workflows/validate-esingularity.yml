name: Validate eSingularity

on:
  pull_request:
    paths:
      - 'modules/foundups/esingularity/**'
      - 'modules/foundups/foundup_registry.json'
      - 'public/f/public_catalog.json'
      - 'public/index.html'
      - 'public/js/reddog-public-client.js'
      - 'public/member/js/red-dog-concierge.js'
      - 'public/member/tests/reddog-public-client.mjs'
      - 'public/member/tests/test_red_dog_concierge.py'
      - '.github/workflows/validate-esingularity.yml'
  push:
    branches: [main]
    paths:
      - 'modules/foundups/esingularity/**'
      - 'modules/foundups/foundup_registry.json'
      - 'public/f/public_catalog.json'
      - 'public/index.html'
      - 'public/js/reddog-public-client.js'
      - 'public/member/js/red-dog-concierge.js'
      - 'public/member/tests/reddog-public-client.mjs'
      - 'public/member/tests/test_red_dog_concierge.py'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Python test dependency
        run: python -m pip install pytest

      - name: Validate FoundUp contracts
        run: python -m pytest modules/foundups/esingularity/tests public/member/tests/test_red_dog_concierge.py -q

      - name: Validate public catalog projection
        run: python -m modules.foundups.public_catalog_projector --validate

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22.13.0'
          cache: npm
          cache-dependency-path: modules/foundups/esingularity/frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: modules/foundups/esingularity/frontend
        run: npm ci

      - name: Lint frontend
        working-directory: modules/foundups/esingularity/frontend
        run: npm run lint

      - name: Validate public admission and stakeholder boundaries
        working-directory: modules/foundups/esingularity/frontend
        run: node --test tests/*.test.mjs

      - name: Validate shared Red Dog browser transport
        run: node public/member/tests/reddog-public-client.mjs

      - name: Check frontend types
        working-directory: modules/foundups/esingularity/frontend
        run: npx tsc --noEmit

      - name: Build frontend
        working-directory: modules/foundups/esingularity/frontend
        run: npm run build
