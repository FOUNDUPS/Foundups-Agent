# Progressive Execution Agent

**Status:** Incubating FoundUp / SPECIFIED
**Working name:** Provisional

## Purpose

Guide a founder through complex real-world FoundUp execution by maintaining a nonlinear dependency/evidence model internally while exposing only the highest-value verified next action to the human operator.

## Core Invariant

The agent sees the whole project. The founder sees the smallest useful next action.

## PoC

Use the Fukui E-Singularity community-feasibility workflow to prove one recursive loop:

`evidence gap -> capability resolution -> guided village interview -> evidence receipt -> ledger/graph update -> next-best-action`

## Runtime Boundary

This FoundUp does not create a second orchestration or SKILLz runtime. It requests capabilities; WRE/WSP 95 resolves and executes validated SKILLz.

## Intake Packet

See `docs/foundups/intake/progressive_execution_agent/`.
