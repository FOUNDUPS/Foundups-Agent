# AmIBot - Manifest Draft

## Registry Fields (Draft)

This intake proposal is retained for provenance. The current declarative
[manifest](../../foundup_manifest.json) and [registry](../../../foundup_registry.json)
now own registration state. Public/runtime admission remains downstream.

| Field | Value |
|---|---|
| foundup_id | detect_ai |
| display_name | AmIBot |
| entity_type | skeleton_candidate |
| module_path | modules/foundups/detect_ai |
| stage | incubating |
| tier | F0_DAE |
| implementation_status | SPECIFIED |
| token_status | TOKEN_DEFERRED |
| token_symbol | null |
| poc_status | idea |
| public_surface_status | hidden |
| public_url_or_route | null |
| next_slice | DETECT_AI_PREFLIGHT_AND_POC_CONTRACT_PHASE1 |

## Notes
This is Markdown intake, not a runtime contract. The entity is a consumer-facing
token-intended candidate; no token economics are operational. Registration
preserves the existing `detect_ai` identity and AmIBot name after current-repository
duplicate checks. Human or Not is an external comparator, not an asserted lineage
parent and not a source-code dependency.

No wallet, DNS, chain, active route, DAO, CABR-ready, payout-ready or investor
entitlement is created. Registry schema, module onboarding and WSP 104 namespace
contracts govern the declarative registration; full runtime readiness is unverified.

## Naming continuity

Display name is AmIBot; tagline is "Can you detect AI?". `detect_ai`, its module path and next-slice identifier remain stable. The older working name is historical provenance. No domain availability, trademark clearance, route activation or registry promotion is claimed.
