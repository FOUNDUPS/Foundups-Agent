# Can You Detect AI? - Intake Source

## Source Type
spoken_012

## Raw Input Summary
012 proposes a game headed "Can you detect AI?". A participant joins and is randomly matched with another person or an AI. Players classify their partner and earn scores. The concept includes human and agent detector leaderboards, comparisons across proprietary and small open models, FoundUp token rewards, possible Discord distribution, and permissioned lessons useful to Red Dog. 012 requests Outcome -> Solution -> Pain, POC -> Prototype -> MVP, and correct monorepo placement under the existing FoundUp protocol.

## Inferred FoundUp Name
Can You Detect AI? (working title, not a cleared trademark or purchased domain).

## Proposed FoundUp ID
detect_ai

## Assumptions
- Browser-first product; Discord is an optional acquisition and embedding surface.
- Initial pilot is invite-only, adult, text-only, with a single declared language per cohort.
- Anonymous practice and authenticated ranked participation remain distinct.
- Token utility is intended, but issuance, supply, price, rewards budget, jurisdiction and redemption are undecided.
- Suggested pilot numbers and latency targets are proposed acceptance criteria, not observations.

## Unresolved Questions
These are downstream gates, not requests to repeat the idea: current reusable matchmaking/auth interfaces; source-bound HoloIndex results; full catalog duplication review; pilot operator and moderation coverage; exact approved model revisions and licenses; hosting budget; privacy/retention jurisdiction; authorized reward policy.

## Duplicate Discovery Status
POSSIBLE_DUPLICATE - discovery incomplete; no matching game found in the searches performed. Do not assert unique ownership or mint/register on this evidence.

## Provenance Note
Source: this conversation, 2026-09-15. Public research was checked the same day. Repository baseline: `FOUNDUPS/Foundups-Agent`, main `b12d75400b3ff819d42c191f87c46e3ed3022e74`.

Read: root AGENTS.md; WSP 00 bootstrap contract; WSP 97 execution contract; canonical WSP 109; FoundUps README, domain canonical index, registry and module directory; RedDog historical BOOTSTRAP; existing OpenRouter catalog-discovery source references. The RedDog archive identifies itself as historical, not live runtime authority.

Lexical searches: `WSP_109`, `INTAKE_SOURCE.md`, `turing`, `human or not`, `detect_ai`, and issue phrase `detect AI`. Results contained no matching FoundUp; an unrelated `_detect_ai_module_type` function is not this product. Search references matched the inspected main revision.

Coverage limits: registry output was large; targeted inspection found no `detect` match, but this is not a full semantic audit. Catalogs `public/member/mall-catalog.json`, `public/member/mall-video-catalog.json`, and `public/f/portfolio_data.json` were not independently read. No external-repository portfolio sweep or HoloIndex query ran. Container GitHub access failed DNS resolution; connector reads remained available. No claim of a passing WSP 00 runtime gate or active worker dispatch is made.

Before build: rerun source-bound HoloIndex (or its governed lexical fallback), inspect all listed catalogs and known external references, verify module uniqueness, and record current-source receipts. Preserve this intake even if the proper result is an update/fork rather than a new venture.
