# antifaFM YouTube Live Broadcaster
# Dependencies for streaming radio to YouTube Live

# Process monitoring (optional but recommended)
psutil>=5.9.0

# Discord voice lane (optional — when ANTIFAFM_DISCORD_VOICE_ENABLED=1)
discord.py>=2.3.0
PyNaCl>=1.5.0

# System requirement (not pip installable):
# FFmpeg - install via: choco install ffmpeg (Windows) or apt install ffmpeg (Linux)
